#include <iostream>
#include <string>

using namespace std;
};

int main() {
    int a[4][2];
    int b[4][2];

    cin >> a[0][0] >> a[0][1] >> a[1][0] >> a[1][1] >> a[2][0] >> a[2][1] >> a[3][0] >> a[3][1];
    cin >> b[0][0] >> b[0][1] >> b[1][0] >> b[1][1] >> b[2][0] >> b[2][1] >> b[3][0] >> b[3][1];

    for (int i = 0; i < 4; i++) {
        if (b[i][0] < 
    }
}